## Why
Automatically create GitHub releases when merging changes via speclife_merge. Currently, the merge workflow bumps npm package versions but doesn't create corresponding GitHub releases, causing version drift between npm and GitHub.

## What Changes
- [List the changes to be made]

## Impact
- Affected specs: [list capabilities]
- Affected code: [key files/systems]
